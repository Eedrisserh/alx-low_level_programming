#include "main.h"
#include <string.h>
/**
 * strlen - Function that prints the length of a character
 * @s: is the varaible that stores the character
 * return nothing
 */
int _strlen(char *s)
{
	strlen(*s);
}
