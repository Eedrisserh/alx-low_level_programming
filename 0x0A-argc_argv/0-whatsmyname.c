#include <stdio.h>

/**
 * main - Program to print its name
 * @argc: counter for the number of arguments
 * @argv: Array that stores the grguments
 * Return: on success return 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
g(void)argc;
	return (0);
}
