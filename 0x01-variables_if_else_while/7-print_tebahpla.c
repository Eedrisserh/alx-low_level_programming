#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
OAOAOA  *  main - Entry point
  *
  * Return: Always 0 (Success)
  */
	int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
{

	putchar(c);
}

	putchar(10); /* this is an ascii code for new line*/

	return (0);
}
